// <i calss="bi bi-trash"></i>

//생성자 함수
function memo ( num, content , importance, ){
    this.num=num;
    this.content=content;
    this.importance=importance;
}
function importStyle(text, color ){
    this.text=text;
    this.color=color;
}